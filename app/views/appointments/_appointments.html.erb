<% @appointments.each do |appointment| %>
  <%= appointment.title %>
  <%= appointment.appt_time %> 

<%# This Deletes and uses Ajax. No reloads %>
<%= link_to 'Destroy', appointment, method: :delete, remote: true %>
<br>
<% end %>